---
description: 需求分析命令
---

# zhuhui_test

> 需求分析命令，将需求文档转换为技术规格

**角色能力**: 需求分析

---

[NO_COMPRESS_START]
以下这部分内容非常重要，请不要进行压缩。

## 角色提示词

# 任务名称

需求分析

# 任务描述

## 全局约束

- **一次性自动完成**，无需用户干预
- 全程自动化，不询问用户
- **禁止编造、猜测或假设**接口路径、代码位置等信息
- 所有代码引用必须指向真实代码文件，禁止引用 `docs/` 下的文档
- 上下文窗口会在接近限制时自动压缩，不要因为Token预算问题提前停止任务
- 最终只输出一个需求分析文档

## 处理步骤

### 步骤1: 验证输入

从命令参数中获取需求文档路径，并进行验证：

验证要求：
- 确认参数中指定的文件或目录路径存在
- 确认该路径包含需求文档（XMind、Markdown等格式）
- 提取需求名称用于输出文件命名
- 验证代码仓库存在：项目根目录下除了 `.claude`、`README.md`、`doc` 之外的其他文件夹

如果验证失败，给出明确错误提示并终止执行。

### 步骤2: 调用技能生成需求分析文档

使用 Skill 工具调用 `requirement-analyzer` 技能：

```
Skill: requirement-analyzer
```

该技能将自动完成：
- 解析需求文档
- 搜索真实代码并建立索引
- 功能点分类与映射（增量/全新、接口/后台、设计需求）
- 生成规范的后端需求分析文档

输出文件：`doc/requirement/requirement.md`

# 工具说明

- **Bash**: 用于执行文件扫描命令
- **Skill**: 调用 `requirement-analyzer` 技能执行核心分析任务

# 输入说明

## 规则

暂无

## 知识库索引

- **代码仓库知识库**：项目根目录下各代码仓库文件夹（排除 `.claude`、`README.md`、`doc`）的 `doc/kb/` 目录下的知识库

# 输出模板

- @.claude/skills/requirement-analyzer/references/output-template.md

# 全局补充输入（动态补充）

## 命令参数

```
/zhuhui_test <需求路径> [补充说明]
```

- `<需求路径>` (必选): 相对于仓库根目录的完整路径，可以是文件或目录，例如 `docs/requirements/input/需求文档.xmind` 或 `docs/requirements/input/需求目录/`
- `[补充说明]` (可选): 用户提供的额外上下文或特殊要求

## 使用说明

### 参数验证

- 如果未提供需求路径，必须立即报错并提示用户：
  ```
  错误：缺少必选参数 <需求路径>
  使用方式：/zhuhui_test <需求路径> [补充说明]
  示例：/zhuhui_test docs/requirements/input/需求文档.xmind
  ```

### 处理方式

- 直接使用用户提供的完整路径
- 验证该路径存在且包含需求文档
- 如果用户提供了补充说明，在执行步骤2调用 `requirement-analyzer` 技能时，将补充说明作为额外上下文传递
- 补充说明会影响：
  - 功能点的优先级判断
  - 复杂度评估
  - 风险分析
  - 技术方案建议

### 默认行为

如果未提供补充说明，则完全基于需求文档和代码仓库进行自动分析。

## 输入目录

请在以下目录准备输入:

- `agent-rules\zhuhui_test\输入/`

## 输出目录

产出将保存到:

- `agent-rules\zhuhui_test\产出/`

## 使用方法

1. 在输入目录准备好需求文档
2. 运行命令: `/zhuhui_test`
3. AI 将基于角色规则和提示词工作
4. 产出将保存到输出目录

---
*该命令由 agent-rules 自动生成*

[NO_COMPRESS_END]
